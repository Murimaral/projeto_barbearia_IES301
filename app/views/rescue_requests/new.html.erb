<section class="rescue-request_form">
  <h3>Criar pedido de resgate</h3>
  <% if @user_claims_ownership %>
    <%= render 'by_owner' %>
  <% else %>
    <%= render 'by_rescuer' %>
  <% end %>
</section>
