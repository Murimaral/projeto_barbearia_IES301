<p>
  Escreva uma mensagem para <%= @found_pet.user.name %> sobre o anúncio <%= link_to @found_pet.name, @found_pet %> e forneça mais detalhes sobre seu pet perdido e sobre você.
</p>
<% if flash[:alert] %>
    <%= flash[:alert] %>
<% end %>
<%= form_for @rescue_request do |form| %>
  <%= form.hidden_field :found_pet_id, value: @found_pet.id %>
  <%= form.hidden_field :rescuer_id, value: @found_pet.user_id %>
  <%= form.hidden_field :owner_id, value: current_user.id %>
  <%= form.hidden_field :created_by_owner, value: true %>
  <%= form.hidden_field :status, value: 'open' %>

  <div class="input-field ">
    <select name="rescue_request[lost_pet_id]" id="rescue_request_lost_pet_id"  class="icons">
      <option value="" disabled selected>Clique para selecionar</option>
      <% @my_pets.each do |pet| %>
        <option value="<%= pet.id %>" data-icon="<%= pet.image %>"><%= pet.name %></option>
      <% end %>
    </select>
    <label>Já cadastrou seu pet na plataforma?</label>
  </div>

  <div class="file-field input-field">
    <p>Você pode anexar uma foto do seu pet abaixo, para auxiliar na identificação:</p>
    <div class="btn red lighten-1">
      <i class="large material-icons">add_a_photo</i>
      <%= form.file_field :image %>
    </div>
    <div class="file-path-wrapper">
      <input class="file-path validate" type="text">
    </div>
  </div>

  <p>Escreva abaixo o máximo de informações sobre seu pet! Procure incluir descrição física, data e local em que foi visto pela última vez.</p>
  <div class="input-field">
    <%= form.text_area :message, class: 'materialize-textarea' %>
    <label for="message">Corpo da mensagem</label>
  </div>

  <p><%= @found_pet.user.name %> será capaz de visualizar seu número de telefone.</p>

  <div class="center-align">
    <%= form.submit 'Enviar', class: 'red lighten-1' %>
  </div>
<% end %>
